name: wasp build

on:
  push:
    branches: [ "opensaas" ]
  
jobs:
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: wasp build